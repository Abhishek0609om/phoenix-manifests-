apiVersion: apps/v1
kind: Deployment
metadata:
  name: phoenix-deployment
  labels:
    app: phoenix-app

# launch 2 replicas of the application
spec:
  replicas: 2
  selector:
    matchLabels:
      app: phoenix-app
  template:
    metadata:
      labels:
        app: phoenix-app
    spec:
      containers:
      - name: phoenix-app
        image: 734984527933.dkr.ecr.ap-south-1.amazonaws.com/phoenix-app:b476683dd84005de1694697e2d5bf9571aa950b0
        imagePullPolicy: Always
        ports:
        - containerPort: 5000 
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "250m"
            memory: "256Mi"
# trigger ci
