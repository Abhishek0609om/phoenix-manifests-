apiVersion: apps/v1
kind: Deployment
metadata:
  name: phoenix-deployment
  labels:
    app: phoenix-app

# launch 2 replicas of the application
spec:
  replicas: 2
  selector:
    matchLabels:
      app: phoenix-app
  template:
    metadata:
      labels:
        app: phoenix-app
    spec:
      containers:
      - name: phoenix-app
        image: 734984527933.dkr.ecr.ap-south-1.amazonaws.com/phoenix-app:e27d65dce4247e7b516b9374556192f310d8f824
        imagePullPolicy: Always
        ports:
        - containerPort: 5000 
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "250m"
            memory: "256Mi"
# trigger ci
